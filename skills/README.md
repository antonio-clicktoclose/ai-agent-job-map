# Skill files

Each file is a self-contained Claude Code skill. Drop the ones you need into your
Claude Code `skills/` directory. Use them as starting contracts and edit them to
match how your business actually runs.

Structure:

    skills/<department>/<job>.md

Every job names its inputs, what Claude should do, and the output contract.
